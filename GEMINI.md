@AGENTS.md

Gemini CLI specifics: there are no slash commands for the workflows. When the user types `onboard`,
`ingest`, `calibrate`, `evaluate`, or `apply <ID>`, open the matching file in `.claude/commands/` and
follow it step by step.
