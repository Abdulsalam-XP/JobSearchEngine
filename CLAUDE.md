@AGENTS.md

Claude Code specifics: the five workflows in `.claude/commands/` are available as slash commands
(`/onboard`, `/ingest`, `/calibrate`, `/evaluate`, `/apply <ID>`). Use the multiple-choice question tool
wherever a workflow says to ask which candidate.
